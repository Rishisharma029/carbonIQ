/**
 * @deprecated — Use `import { authenticate } from './authenticate.js'` instead.
 * This file is kept as a backward-compatible re-export for existing route files.
 */
export { authenticate as protect, authenticate } from './authenticate.js'
export { protect as default } from './authenticate.js'
